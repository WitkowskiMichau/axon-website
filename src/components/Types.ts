export interface PriceItemProps {
    title: string;
    price: string;
    features: string[];
}
export interface TileProps {
    title: string;
    description: string;
    icon: string;
    url?: string;
}